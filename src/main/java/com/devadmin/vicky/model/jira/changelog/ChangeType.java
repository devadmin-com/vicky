package com.devadmin.vicky.model.jira.changelog;

/**
 * Type of change happened on task ASSIGN is type of task change which happened because of
 * assignment
 */
public enum ChangeType {
    ASSIGN,
    DEFAULT;
}
